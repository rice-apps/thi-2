import Joi from "joi"

module.exports = {
    create: Joi.object({

    }),
    update: Joi.object({

    }),
    delete: Joi.object({

    }),
    findAll: Joi.object({

    }),
    findById: Joi.object({

    })
};